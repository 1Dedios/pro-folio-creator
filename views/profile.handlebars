<div class="container profile-header">
    <div class="card">
        <h1>{{user.username}}</h1>
        <p class="muted">{{user.email}}</p>
    </div>
</div>

{{> portfolios user=user portfolios=portfolios}}

<div class="site-footer">
    &copy; <span id="year">{{currentYear}}</span> ProFolio
</div>